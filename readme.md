
## Directory Structure
```plain
CompositeSort
Heap/
├── Heap Sort(average runtimes).cpp
└── Heap Sort(worst-time criterion).cpp
Insertion/
├── Insertion Sort(average runtimes).cpp
└── Insertion Sort(worst-time criterion).cpp
Merge/
├── Merge Sort(average runtimes).cpp
├── Merge Sort(worst-time criterion).cpp
├── MergeSortCompare.cpp
└── output.txt
Quick/
├── Quick Sort(average runtimes).cpp
└── Quick Sort(worst-time criterion).cpp
```